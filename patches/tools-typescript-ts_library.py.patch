diff --git a/tools/typescript/ts_library.py b/tools/typescript/ts_library.py
index 4dbbb4455516a..1e75f36346ece 100644
--- a/tools/typescript/ts_library.py
+++ b/tools/typescript/ts_library.py
@@ -148,6 +148,9 @@ def main(argv):
     # Source .ts files are always resolved as being relative to |root_dir|.
     tsconfig['files'].extend([os.path.join(root_dir, f) for f in args.in_files])
 
+  # Include overridden files from Upstream
+  tsconfig['include'] = ["preprocessed/**/*-chromium*.ts"]
+
   if args.definitions is not None:
     for d in args.definitions:
       assert d.endswith(
@@ -277,5 +280,6 @@ def main(argv):
       json.dump(manifest_data, manifest_file)
 
 
+from brave_chromium_utils import inline_chromium_src_override; inline_chromium_src_override(globals(), locals())
 if __name__ == '__main__':
   main(sys.argv[1:])
